import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0504eb',
    justifyContent: 'center',
  },
  title: { color: '#ffffff', fontSize: 24, textAlign: 'center' },
})

export default styles
